- {"tool":"calendar_list_appointments","arguments":{"date_from":1712448000,"date_to":1715126400,"limit":25}}
  Use to list synced calendar appointments in a specific date range other than today or tomorrow. date_from and date_to must be different timestamps spanning a real range; never set them equal to each other. Compute them as Unix timestamps in seconds from the Current Date And Time given in this prompt; never reuse the example numbers above, they are illustrative only and not the current date. For "today" or "tomorrow" questions use list_today_appointments or list_tomorrow_appointments instead. For "what's my next meeting" use next_appointment instead. Every appointment a calendar tool returns is shown with a trailing "[event_id: N]". The very next time the user asks a follow-up about one specific appointment already shown in this conversation (e.g. "who's attending that meeting?"), your entire tool call must be exactly {"tool":"calendar_list_appointments","arguments":{"event_id":N}} using that same N — do not also include date_from, date_to, or a text filter, and do not guess a new date range instead.
